# 博文编写方案 · 实施文档

> 基于《博文编写方案分析.md》与当前项目现状整理（单语言 `src/content/blog/`、COS 图床已就绪、Vercel 部署）。
> **本次确认的实施范围：P1（Sveltia CMS）为主，P2（Obsidian）暂缓，作为附录备查。**
>
> ⚠️ **后续变更：中英双语已移除。** 原方案的 `zh/`+`en/` 双目录、`blog-en` 集合、`translationKey` 关联、`[...lang]` 动态路由和导航栏语言切换按钮全部删除。
> 现在只有一份中文内容：文章平铺在 `src/content/blog/*.md`，文件名即 URL slug（建议英文短横线），CMS 只有一个「博客文章」集合。
> 旧的 `/en/*` 链接由 `astro.config.mjs` 的 `redirects` 配置 301 回对应中文页（**不是** `vercel.json`：Astro 的 Vercel 适配器走 Build Output API，重定向必须由适配器写进 `.vercel/output/config.json` 才生效）。下文相关段落已同步更新。

---

## 一、目标与架构

多种写博客方式并存，统一落到 GitHub 仓库，由 Vercel 检测变更自动构建：

```
┌──────────────────────────────┐
│ 写博客的几种入口              │
├──────────────────────────────┤
│ ① VSCode（现状，不动）        │──┐
│ ② Sveltia CMS（本次新增）     │──┤ 全部写入
│ ③ GitHub.dev / 网页编辑       │──┤
│ ④ Obsidian（P2 暂缓）         │──┘
└──────────────────────────────┘
              │
              ▼
   fartmonarch/astro_blog 仓库（src/content/blog/*.md(x)）
              │
              ▼
        Vercel 检测 push → 自动构建部署
```

| 入口 | 状态 | 本次动作 |
|---|---|---|
| VSCode + Git Push（方案 A） | 已在使用 | 无 |
| GitHub 网页 / github.dev（方案 D） | 天然可用 | 无（写进日常流程） |
| Sveltia CMS（方案 C） | 已实现 | P1 已完成 |
| Obsidian（方案 B） | 未实现 | P2 暂缓（见附录三） |

---

## 二、P1：Sveltia CMS 实施步骤

### 2.0 前置检查

- [x] `fartmonarch/astro_blog` 仓库存在，默认分支为 `main`
- [x] 本地 `astro dev` 可启动（`public/` 会映射到站点根路径，`/admin/` 可直接访问）
- [x] 已确认现有文章是纯 Markdown 内容（无 MDX 组件语法），见 2.4 风险表

### 2.1 准备 `public/admin/` 三个文件

```
public/admin/
├── index.html        ← 后台入口页
├── sveltia-cms.js    ← 官方单文件（v0.191.1，约 1.9MB，无需构建）
└── config.yml        ← CMS 配置
```

**下载 sveltia-cms.js**（GitHub Release 无独立资产，从 npm 包走 CDN 获取）：

```bash
curl -L -o public/admin/sveltia-cms.js https://cdn.jsdelivr.net/npm/@sveltia/cms@0.191.1/dist/sveltia-cms.js
```

**index.html**：

```html
<!doctype html>
<html lang="zh-CN">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <meta name="robots" content="noindex" />
  <title>博客管理后台</title>
</head>
<body>
  <script src="sveltia-cms.js"></script>
</body>
</html>
```

**config.yml**（对照现有 `src/content.config.ts` 的 schema 逐字段映射）：

```yaml
backend:
  name: github
  repo: fartmonarch/astro_blog
  branch: main
  # 认证：GitHub Personal Access Token（登录页 "Sign In with Token"），无需 client_id，见 2.2

# 图片继续走 COS 图床外链，CMS 内不启用仓库图片上传
# （media_folder / public_folder 为满足配置要求而保留）
media_folder: "public/images"
public_folder: "/images"

site_url: https://fartmonarch.xyz

collections:
  - name: "blog"
    label: "博客文章"
    folder: "src/content/blog"
    create: true
    delete: true
    extension: md
    format: frontmatter
    slug: "{{slug}}"
    editor:
      preview: false          # Astro 无现成预览 iframe，关闭避免报错
    fields:
      - { label: "标题", name: "title", widget: "string" }
      - { label: "描述", name: "description", widget: "text" }
      - { label: "发布日期", name: "pubDate", widget: "datetime", format: "YYYY-MM-DD", date_format: "YYYY-MM-DD", time_format: false }
      - { label: "更新日期（可选）", name: "updatedDate", widget: "datetime", format: "YYYY-MM-DD", date_format: "YYYY-MM-DD", time_format: false, required: false }
      - { label: "标签", name: "tags", widget: "list", default: [] }
      - { label: "正文", name: "body", widget: "markdown" }
```

要点：

- **单 collection**：`blog` 指向 `src/content/blog/`，`post.id` 就是去掉扩展名的文件名，URL 形如 `/blog/<文件名>/`。所以新建文章时建议用英文短横线命名（中文标题不影响显示，只影响文件名）。
- **`extension: md`**：CMS 新建文章保存为 `.md`。当前内容无 MDX 特性，Astro 的 `**/*.{md,mdx}` 两种扩展名都能渲染；既有的 `hello-world.mdx` 保持 VSCode 维护（见 2.4）。
- **`pubDate` 用 `format: "YYYY-MM-DD"`**：与现有 frontmatter 风格一致，`z.coerce.date()` 可直接解析。
- **不启用 CMS 图片上传**：图片继续 PicGo → COS → 外链，编辑器里直接粘贴链接。

### 2.2 GitHub 认证：Personal Access Token（无需 OAuth App，约 3 分钟）

[官方文档](https://sveltiacms.app/en/docs/backends/github)明确：单用户/小团队场景用 PAT 最简，**不需要创建 OAuth App、不需要改配置**（OAuth 授权码流程仅多人协作才需要，且要额外部署 Cloudflare Worker 认证服务）。

1. 打开 [GitHub → Settings → Developer settings → Personal access tokens → Tokens (classic)](https://github.com/settings/tokens) → **Generate new token (classic)**
2. 勾选权限：**`repo`**（完整仓库读写，CMS 提交文章需要）；有效期自选（建议 90 天）
3. 生成后复制 token（**只显示一次**，妥善保存）
4. 打开 `https://fartmonarch.xyz/admin/` → 点 **Sign in with Token** → 粘贴 token
5. Token 只保存在浏览器 local storage，不落代码、不上服务器；疑似泄露时到 GitHub 设置 revoke 重建即可

### 2.3 本地验证清单

1. `astro dev`，打开 `http://localhost:4321/admin/`
2. 点 **Sign in with Token** → 粘贴 PAT → 进入后台
3. 确认左侧出现「博客文章」集合（内部名 `blog`）
   - `.mdx` 文章（`hello-world.mdx`）不出现在列表里属预期行为（CMS 只管 `.md`）
4. 新建一篇测试文章：
   - 填标题/描述/日期/标签，正文写几行 Markdown
   - 保存 → 自动 commit + push 到 `main`
5. `git log` 确认 CMS 产生独立 commit；检查新文件 `src/content/blog/<slug>.md` 的 frontmatter 与 schema 一致
6. 若登录或列表异常：见 2.4 风险表

### 2.4 风险与对策

| 风险 | 现象 | 对策 |
|---|---|---|
| Sveltia 不识别 `.mdx`（[Issue #79](https://github.com/sveltia/sveltia-cms/issues/79)，0.8.4 时代 bug，**已关闭**） | 文件列表里看不到既有 .mdx | 本方案用 `extension: md`，新文章走 `.md` 不受影响；`src/content/blog/hello-world.mdx` 保持 VSCode 维护。若希望 CMS 统一管理，把它转成 `.md` 即可全覆盖 |
| 新建文章 slug 与既有文件重名 | 路由重复 / 构建报错 | 新建时用英文短横线命名，避开既有文件名 |
| 中文标题自动生成中文文件名 | URL 出现一长串 `%E6%B8%85%E7%90%86…`，分享和 SEO 不友好 | 保存后用编辑器右上 **⋮ → Edit Slug** 改成英文短横线；Git 会记为 rename，历史不丢（旧链接会失效） |
| PAT 泄露 | 他人可修改仓库内容 | Token 只在浏览器本地；疑似泄露到 GitHub 设置 revoke 并重建 |
| frontmatter 不符合 schema | Vercel 构建失败（红） | 典型原因：**CMS 保存时会把空的可选字段写成 `''`**（如 `updatedDate: ''`），zod 4 会拒绝。已在 `content.config.ts` 用 `z.preprocess` 把空串转 `undefined` 根治，正常情况不会再触发；若仍报错，按报错提示修对应字段 |
| `/admin` 公网可访问 | 任何人都能打开登录页 | 个人博客可接受（无账号无法提交）；如需加固见附录一 |

### 2.5 日常写作流程

- **快速发文**：打开 `https://fartmonarch.xyz/admin/` → 博客文章 → New → 填字段 → 正文图片用 COS 外链 `![](https://fartmonarch-cos-1344165548.cos.ap-shanghai.myqcloud.com/img/xxx.webp)` → Publish
- **文件名**：默认由标题生成（中文标题 → 中文文件名）；想留干净的英文 URL，保存后 ⋮ → Edit Slug 改成英文短横线，例如 `hibit-uninstaller-registry-cleanup-issues`
- **修改已有文章**：编辑保存 → 有实质更新记得填 `updatedDate` → Vercel 几十秒后自动上线
- **临时小改 / 手机**：GitHub 仓库按 `.` 键进 github.dev 直接改，或 GitHub 网页编辑
- **深度写作**：继续 VSCode（方案 A），现有 PicGo 图床流程不变

### 2.6 线上部署验证

1. 提交 admin 三件套 → push → Vercel 构建成功
2. 打开 `https://fartmonarch.xyz/admin/` 用 PAT 登录 → 发一篇真实测试文章 → 确认线上出现
3. 第五节验收清单全过即完成 P1

---

## 三、P2：Obsidian 方案（暂缓，附录）

> 决策依据：当前偶尔用 Obsidian；本地深度写作已由 VSCode + PicGo 覆盖，故**先不做**。若未来 Obsidian 成为主力笔记工具，按此步骤实施。

### 3.1 总体链路

```
Obsidian 编辑 → Obsidian Git 插件自动 push → fartmonarch/blog-content 仓库
                                                ↓（仓库内 GitHub Actions）
                                        Vercel Deploy Hook POST
                                                ↓
                          Vercel 重新构建：fetch-content.sh 拉取 → astro build
```

### 3.2 步骤

1. **建内容仓库**：GitHub 新建 `fartmonarch/blog-content`（可 private），结构：
   ```
   blog-content/
   ├── *.md                        ← 单语言（中文），文件名即 slug
   └── images/（可选，Obsidian 粘贴的图片）
   ```
2. **Obsidian 配置**：vault 指向本地 clone；安装 [obsidian-git](https://github.com/Vinzent03/obsidian-git)，`Vault backup interval` 设 **30+ 分钟**（避免频繁触发 Vercel 构建，消耗 [Hobby 构建配额](https://vercel.com/docs/plans/hobby)）
3. **项目接入**：
   - `scripts/fetch-content.sh`：
     ```bash
     #!/bin/bash
     set -e
     CONTENT_REPO="https://github.com/fartmonarch/blog-content.git"
     CONTENT_DIR="src/content/blog-external"
     if [ -d "$CONTENT_DIR/.git" ]; then
       git -C "$CONTENT_DIR" pull --ff-only origin main
     else
       git clone --depth 1 "$CONTENT_REPO" "$CONTENT_DIR"
     fi
     ```
   - `package.json`：`"build": "bash scripts/fetch-content.sh && astro build"`
   - **内容合并（两种做法二选一）**：
     - **做法 1（零改路由，推荐尝试）**：自定义 loader 把两个 glob 合并进同一个 `blog` collection：
       ```ts
       const localBlog = glob({ pattern: "**/*.{md,mdx}", base: "./src/content/blog" });
       const externalBlog = glob({ pattern: "**/*.{md,mdx}", base: "./src/content/blog-external" });
       const blog = defineCollection({
         loader: async (options) => {
           await Promise.all([localBlog.load(options), externalBlog.load(options)]);
         },
         schema: blogSchema,
       });
       ```
       ⚠️ 实施时验证：两个 glob 写入同一 store 的 id 冲突行为（id 是相对各自 base 的路径，同名文件会撞）。对策：外部仓库文章命名不与本地重名，或加载后改写 id 加前缀。
     - **做法 2（更稳妥）**：保留 `blog`，另加 `blogExternal` collection，新建 `src/lib/posts.ts` 的 `getAllPosts()` 合并两者，把 5 个路由文件（index / archive / tags / tags/[tag] / blog/[slug] / rss）里的 `getCollection("blog")` 换成 `getAllPosts()`（每处 1 行）。
4. **触发构建**：
   - Vercel Dashboard → 项目 → Settings → Git → Deploy Hooks → 创建 `content-update` → 复制 URL
   - `blog-content` 仓库加 `.github/workflows/trigger-deploy.yml`：
     ```yaml
     name: Trigger Vercel Deploy
     on:
       push:
         branches: [main]
     jobs:
       deploy:
         runs-on: ubuntu-latest
         steps:
           - run: curl -X POST "${{ secrets.VERCEL_DEPLOY_HOOK }}"
     ```
   - 仓库 Settings → Secrets and variables → Actions → 新增 `VERCEL_DEPLOY_HOOK`
5. **与 P1 共存**：VSCode / CMS / Obsidian 三条路写的内容最终在同一构建里合并，互不冲突。

### 3.3 P2 风险

- 5 个独立环节串联（插件 → 内容仓库 → fetch 脚本 → Deploy Hook → Actions），任一静默失败都表现为"博客没更新"，排查链路长
- 自动 push 频率 × Vercel Hobby 构建配额（见 [Vercel Hobby 文档](https://vercel.com/docs/plans/hobby)）
- Obsidian Git 移动端同步不稳定，手机场景仍靠方案 C / D
- 内容语言：目前只有中文，vault 内直接平铺 `*.md` 即可（将来若真要加英文，再按语言分子目录）

---

## 四、验收清单（P1 完成标准）

- [ ] `https://fartmonarch.xyz/admin/` 可打开登录页
- [ ] GitHub PAT（"Sign in with Token"）登录成功
- [ ] 「博客文章」集合可见
- [ ] 新建文章保存后，仓库出现 `src/content/blog/<slug>.md`，frontmatter 含 `title / description / pubDate / tags`，且 `pubDate` 为 `YYYY-MM-DD`
- [ ] 线上 `/blog/<slug>/` 可访问；旧 `/en/*` 链接 301 到对应中文页
- [ ] 正文中的 COS 图片外链正常显示
- [ ] 删除测试文章后线上同步移除

---

## 五、中英双语移除记录（本期完成）

> 逐文件、逐层的详细改动、URL 对照、验证记录与回滚方法，见 **[《中英双语移除变更记录.md》](./中英双语移除变更记录.md)**。
> 下面只留一张速查表。

删除项一览，便于日后追溯：

| 层 | 处理 |
|---|---|
| 内容 | `src/content/blog/zh/*` 拍平到 `src/content/blog/*`；删除 `src/content/blog/en/`、`src/content/pages/en/`，`content/pages/zh/about.md` 上移为 `content/pages/about.md` |
| 路由 | `src/pages/[...lang]/{index,archive,about,rss.xml,blog/[...slug],tags/index,tags/[tag]}` 上移到 `src/pages/` 根并去掉 lang 参数；删除遗留静态页 `src/pages/{en,zh}/about.md` |
| i18n | 删除整个 `src/i18n/`（`ui.ts`、`utils.ts`），删除 `astro.config.mjs` 的 `i18n` 配置块 |
| 组件/文案 | 导航栏语言切换器及其 CSS 变量移除；Header/Footer/Profile/BlogPost/404 文案改为中文字面量；`<html lang="zh-CN">`；`config.ts` 的 `AUTHOR.role/bio` 改为字符串 |
| Schema/CMS | `content.config.ts` 去掉 `translationKey`；`config.yml` 去掉 `blog-en` 集合与 `translationKey` 字段，`folder` 改为 `src/content/blog`；各文章 frontmatter 里的 `translationKey` 已清理 |
| 重定向 | `astro.config.mjs` 增加 `redirects`：`/en`、`/en/about`、`/en/archive`、`/en/tags`、`/en/rss.xml`、`/en/blog/hello-world` 301 到对应中文路径；适配器会写入 `.vercel/output/config.json`。**动态目标（`/[...path]`）不被 Astro 允许，只能逐条列**（带不带结尾斜杠会被归一化成同一条规则，无需写两遍） |

---

## 附录一：可选增强——给 `/admin` 加访问保护

- Vercel Dashboard → 项目 → Settings → Deployment Protection → Password Protection（访问 `/admin` 前需密码）
- 缺点：全站访问者都要密码；仅个人博客场景下可按需开启

## 附录二：参考资料

- [Sveltia CMS GitHub Backend 文档](https://sveltiacms.app/en/docs/backends/github)
- [Sveltia CMS Entry Collections（slug 生成规则）](https://sveltiacms.app/en/docs/collections/entries)
- [Sveltia CMS Content Editor（Edit Slug）](https://sveltiacms.app/en/docs/ui/content-editor)
- [Sveltia CMS + Astro 搭建指南](https://bryanhogan.com/blog/sveltia-cms-astro-blog)
- [sveltia-cms npm 包（@sveltia/cms，单文件下载源）](https://www.npmjs.com/package/@sveltia/cms)
- [Sveltia CMS Issue #79：MDX 识别问题（已关闭）](https://github.com/sveltia/sveltia-cms/issues/79)
- [Astro 官方 Decap CMS 指南](https://docs.astro.build/en/guides/cms/decap-cms/)
- [GitHub Personal Access Tokens](https://github.com/settings/tokens)
- [obsidian-git 插件](https://github.com/Vinzent03/obsidian-git)
- [Vercel Hobby 计划与限额](https://vercel.com/docs/plans/hobby)
